input {
    stdin {
        type = "stdin-type"
    }
    redis {
        type = "redis-queue"
        host = "localhost"
        type = "list"
        key  = "seshat"
    }
}
output {
    stdout {
        debug = true
        debug_format = "json"
    }
}
