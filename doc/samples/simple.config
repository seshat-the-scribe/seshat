input {
    stdin {
        type = "stdin-type"
    }
}

filter {
    simpletest = {}
    simpletest = {}
}

output {
    stdout {
      debug = true
      debug_format = "json"
    }
    stdout {
      debug = true
      debug_format = "json"
    }
}
